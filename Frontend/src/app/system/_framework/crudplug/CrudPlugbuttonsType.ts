import { GridSetings } from "../grid/GridSetings";
import { FormGroup } from "@angular/forms";
import { baseCrudComponent } from "../helppers/baseCrud.component";

export enum CrudPlugbuttonsType {
    
    Add,
    Edit,
    Delete,
    Search,
    Refresh,
    Eraser
}

